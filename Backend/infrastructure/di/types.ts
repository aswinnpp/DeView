// Re-export from shared to maintain backward compatibility with infrastructure code
// Application layer should import from shared/di/types directly
export { TYPES } from '../../shared/di/types.js';
